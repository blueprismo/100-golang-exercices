package main

// import the "testing" library
import (
    "testing"
)

// In the TestReverse function, create a struct called 'testcases' containing 2 variables "in" and "want" of type string

func TestReverse(t *testing.T) {
    testcases := []struct {
    
    }{ 
		// populate 3 cases of strings in the "in" variable, and reverse it in the "want" variable
    }
	// iterate over the testcases and test if the Reversed string is equal to want. If its not raise an error
    
}